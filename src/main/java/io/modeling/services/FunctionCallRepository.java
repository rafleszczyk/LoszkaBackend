package io.modeling.services;
import org.springframework.data.jpa.repository.JpaRepository;
import io.modeling.models.*;

public interface FunctionCallRepository extends JpaRepository<FunctionCallModel, Integer>{

}
